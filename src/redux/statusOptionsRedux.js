//selectors
export const getAllStatusOptions = state => state.statusOptions

// actions

// action creators

const statusOptionsReducer = (statePart = [], action) => {
  switch (action.type) {
    default: 
      return statePart;
  };
};
export default statusOptionsReducer;